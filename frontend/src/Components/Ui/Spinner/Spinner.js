import classes from "./Spinner.module.css"

const Spinner =()=>(
    <div className={classes.Spinner}>
        <div className={classes.ldSpinner}><div></div><div></div><div></div></div>
    </div>
)

export default Spinner